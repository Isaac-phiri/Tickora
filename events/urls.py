from django.urls import path
from .views import *
urlpatterns = [
     
    # path('', EventHomePageListView.as_view(), name="homepage"),

    # path('event_list/', EventListView.as_view(), name='event-list'),
    # path('event_detail/<int:pk>/', EventDetailView.as_view(), name='event-detail'),
    # path('event_create/', EventCreateView.as_view(), name='event-create'),
    # path('event_update/<int:pk>/', EventUpdateView.as_view(), name='event-update'),
    # path('event_delete/<int:pk>/', EventDeleteView.as_view(), name='event-delete'),
    # path('status/<str:status>/', EventStatusListView.as_view(), name='event-status-list'),
    # path('event_search/', EventSearchListView.as_view(), name='event_search'),
    
    
    

    # # AirlineTicket URLs
    # path('airline_tickets/', AirlineTicketListView.as_view(), name='airline_ticket_list'),
    # path('airline_tickets/<int:pk>/', AirlineTicketDetailView.as_view(), name='airline_ticket_detail'),
    # path('airline_tickets/create/', AirlineTicketCreateView.as_view(), name='airline_ticket_create'),
    # path('airline_tickets/<int:pk>/update/', AirlineTicketUpdateView.as_view(), name='airline_ticket_update'),
    # path('airline_tickets/<int:pk>/delete/', AirlineTicketDeleteView.as_view(), name='airline_ticket_delete'),
    
    # # CinemaTicket URLs
    # path('home_cinema/', home, name='home'),
    # path('cinemas/', CinemaListView.as_view(), name='cinema_list'),
    # path('cinemas/<uuid:pk>/', CinemaDetailView.as_view(), name='cinema_detail'),
    # path('movies/', MovieListView.as_view(), name='movie_list'),
    # path('movies/<uuid:pk>/', MovieDetailView.as_view(), name='movie_detail'),
    
    # # BusTicket URLs
    # path('bus_tickets/', BusTicketListView.as_view(), name='bus_ticket_list'),
    # path('bus_tickets/<int:pk>/', BusTicketDetailView.as_view(), name='bus_ticket_detail'),
    # path('bus_tickets/create/', BusTicketCreateView.as_view(), name='bus_ticket_create'),
    # path('bus_tickets/<int:pk>/update/', BusTicketUpdateView.as_view(), name='bus_ticket_update'),
    # path('bus_tickets/<int:pk>/delete/', BusTicketDeleteView.as_view(), name='bus_ticket_delete'),
    
    
]
